
  <div class="col-md-5 col-md-offset-2">
    <% @articles.each do |article| %>
    <h1><%= link_to article.title, article %></h1>
    <div>
      <%= article.body %> - <%= link_to "Delete", article, method: :delete, class:"btn btn-danger" %>
    </div>

    <% end %>
  </div>
